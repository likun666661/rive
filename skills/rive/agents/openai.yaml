interface:
  display_name: "Rive"
  short_description: "Run observable, recoverable mixed-runner workflow DAGs with Rive."
  default_prompt: "Use Rive to turn my objective into a ledger-backed workflow or Work DAG, run node-level OpenCode/Codex workers with scheduler/status/retry/worktree isolation, inspect neutral trace activity when needed, and report the projection-backed result."
